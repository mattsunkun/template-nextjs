/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/template-nextjs",
    output: "export",
    images: { unoptimized: true } ,
}

// const nextConfig = {
//     output: 'export',
//     distDir: 'dist'
// };

export default nextConfig;
